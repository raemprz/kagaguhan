Copy your existing VenPCS image files into this folder before submission. The uploaded backup did not include the image assets, so they were intentionally not fabricated or renamed.

Main image files referenced by the project include:
- venpcslogonew.png
- tablogo.png

Plus the product/banner files referenced by the HTML in the main/images/ paths. Keep the existing filenames so the website continues to load them.
